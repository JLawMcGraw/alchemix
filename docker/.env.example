# AlcheMix Docker Compose Environment Variables
# Copy this file to .env before running docker compose up
# Usage: cp .env.example .env && edit .env with your API key

# =============================================================================
# REQUIRED - OpenAI API Key for MemMachine embeddings
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# =============================================================================
# Database Credentials (match docker-compose.yml defaults - usually no change needed)
# =============================================================================
NEO4J_USER=neo4j
NEO4J_PASSWORD=alchemixpassword
POSTGRES_USER=memmachine
POSTGRES_PASSWORD=memmachinepassword
POSTGRES_DB=memmachine
