/**
 * AlcheMix Store - Re-export
 * Backwards compatibility wrapper for refactored slice-based store
 */

export { useStore } from './store/index';
export type { AppStore as AppState } from './store/index';
